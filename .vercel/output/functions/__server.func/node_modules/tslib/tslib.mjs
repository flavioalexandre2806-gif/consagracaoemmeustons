const __assign = Object.assign || function (t) {
    for (let s, i = 1; i < arguments.length; i++) {
          s = arguments[i];
          for (const p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
    }
    return t;
};
const __rest = function (s, e) {
    const t = {};
    for (const p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function") {
          for (const p of Object.getOwnPropertySymbols(s)) if (e.indexOf(p) < 0 && Object.prototype.propertyIsEnumerable.call(s, p)) t[p] = s[p];
    }
    return t;
};
const __spreadArray = function (to, from, pack) {
    if (pack || arguments.length === 2) for (let i = 0, l = from.length; i < l; i++) if (i in from) to[i] = from[i];
    return to.concat(from);
};
export { __assign, __rest, __spreadArray };
